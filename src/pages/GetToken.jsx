import React, {useEffect} from 'react';

const GetToken = () => {
  useEffect(() => {
    alert('GetToken')
  }, []);

  return (
    <div>
      Яндекс Токен
    </div>
  );
};

export default GetToken;